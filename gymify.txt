npm install firebase
Then, initialize Firebase and begin using the SDKs for the products you'd like to use.

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyCCWGnsr_KMxZErrWrvbFw4vIm93VfrBBo",
  authDomain: "gymify-26ebc.firebaseapp.com",
  projectId: "gymify-26ebc",
  storageBucket: "gymify-26ebc.firebasestorage.app",
  messagingSenderId: "766516910912",
  appId: "1:766516910912:web:0ea23f1ff9868ce34e38a5",
  measurementId: "G-3DC716YKQ2"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);




include:
“Daily Quest Streak”
“Energy Bar” (more like mine craft heart (each has total 4 hearsts if all gone (due to skip or lost in nstreak) then game over then need to restartr the game all xp gone and other acheivement)


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Signup | Gymify</title>
  <link rel="stylesheet" href="css/auth.css" />
  <script type="module" src="js/auth.js" defer></script>
</head>
<body>
  <div class="auth-container">
    <h2>Join Gymify 💪</h2>
    <form id="signup-form">
        <input type="text" id="signup-username" placeholder="Username" required />
      <input type="email" id="signup-email" placeholder="Email" required />
      <input type="password" id="signup-password" placeholder="Password" required />
      <button type="submit">Create Account</button>
    </form>
    <p>Already have an account? <a href="login.html">Login here</a></p>
    <div id="message"></div>
  </div>

  <!-- Firebase SDKs -->
  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.13.1/firebase-app.js";
    import { 
    getAuth, 
    createUserWithEmailAndPassword, 
    updateProfile, 
    onAuthStateChanged 
    } from "https://www.gstatic.com/firebasejs/10.13.1/firebase-auth.js";
    import { firebaseConfig } from "./js/firebase-config.js";

    const app = initializeApp(firebaseConfig);
    const auth = getAuth(app);

    const form = document.getElementById("signup-form");
    const msg = document.getElementById("message");

    form.addEventListener("submit", async (e) => {
    e.preventDefault();

    const username = document.getElementById("signup-username").value;
    const email = document.getElementById("signup-email").value;
    const password = document.getElementById("signup-password").value;

    try {
        // Step 1: Create user
        const userCredential = await createUserWithEmailAndPassword(auth, email, password);
        const user = userCredential.user;

        // Step 2: Update displayName
        await updateProfile(user, { displayName: username });
        console.log("✅ Updated display name:", username);

        // Step 3: Wait a short moment to ensure Firebase syncs
        await new Promise((resolve) => setTimeout(resolve, 800));

        // Step 4: Re-fetch the current user (ensures updated data)
        const refreshedUser = auth.currentUser;
        console.log("🔁 Refreshed user displayName:", refreshedUser.displayName);

        msg.textContent = "Signup successful! Redirecting...";
        setTimeout(() => (window.location.href = "dashboard.html"), 1500);
    } catch (error) {
        console.error("Signup error:", error);
        msg.textContent = error.message;
    }
    });

    // Optional: Prevent signed-in users from seeing signup again
    onAuthStateChanged(auth, (user) => {
    if (user) {
        window.location.href = "dashboard.html";
    }
    });


  </script>
  <script type="module" src="js/redirectIfLoggedIn.js" defer></script>

</body>
</html>
 working sign up